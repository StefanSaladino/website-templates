const header=document.querySelector('[data-header]');const toggle=document.querySelector('.menu-toggle');const mobileMenu=document.querySelector('.mobile-menu');const hero=document.querySelector('[data-hero]');const heroImage=hero?.querySelector('.hero-media img');const reducedMotion=window.matchMedia('(prefers-reduced-motion: reduce)').matches;function syncHeader(){header?.classList.toggle('is-scrolled',window.scrollY>10||document.body.classList.contains('menu-open'))}function closeMenu(){if(!toggle||!mobileMenu)return;toggle.setAttribute('aria-expanded','false');toggle.setAttribute('aria-label','Open navigation');mobileMenu.hidden=true;document.body.classList.remove('menu-open');syncHeader()}toggle?.addEventListener('click',()=>{const open=toggle.getAttribute('aria-expanded')==='true';toggle.setAttribute('aria-expanded',String(!open));toggle.setAttribute('aria-label',open?'Open navigation':'Close navigation');mobileMenu.hidden=open;document.body.classList.toggle('menu-open',!open);syncHeader()});mobileMenu?.querySelectorAll('a').forEach(link=>link.addEventListener('click',closeMenu));window.addEventListener('keydown',e=>{if(e.key==='Escape')closeMenu()});window.addEventListener('resize',()=>{if(innerWidth>980)closeMenu()});window.addEventListener('scroll',syncHeader,{passive:true});syncHeader();if(hero&&heroImage&&!reducedMotion){let frame=null;const update=()=>{frame=null;const rect=hero.getBoundingClientRect();const progress=Math.max(0,Math.min(1,-rect.top/Math.max(rect.height,1)));const travel=innerWidth<=620?42:innerWidth<=980?70:150;heroImage.style.transform=`translate3d(0,${progress*travel}px,0) scale(${innerWidth>980?1.06:1.025})`};const request=()=>{if(frame!==null)return;frame=requestAnimationFrame(update)};addEventListener('scroll',request,{passive:true});addEventListener('resize',request);update()}const reveals=document.querySelectorAll('.reveal');if('IntersectionObserver'in window&&!reducedMotion){const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(!entry.isIntersecting)return;entry.target.classList.add('is-visible');observer.unobserve(entry.target)})},{threshold:.12});reveals.forEach(item=>observer.observe(item))}else{reveals.forEach(item=>item.classList.add('is-visible'))}